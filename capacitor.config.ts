import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.wtecnologia.radioBr',
  appName: 'radiosBr',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
